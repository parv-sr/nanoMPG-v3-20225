name: diamond_gelu
model:
  layer_sizes: [14, 32, 64, 128, 64, 32, 1]
  activation: gelu
  use_batch_norm: true
  dropout: 0.1
  use_residual: true
training:
  epochs: 500
  batch_size: 64
  learning_rate: 0.001
  scheduler: cosine
