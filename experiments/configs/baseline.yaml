name: baseline
model:
  layer_sizes: [14, 16, 16, 8, 1]
  activation: sigmoid
  use_batch_norm: false
  dropout: 0.0
  use_residual: false
training:
  epochs: 500
  batch_size: 64
  learning_rate: 0.001
  scheduler: none
